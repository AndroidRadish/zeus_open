## Decision Rationale

Wave 2 implements the v2 "core engine": the async orchestrator that replaces the manual copy-paste loop of v1, and the FastAPI backend that unifies data access for both GUI and Web frontends. T-004 and T-005 were executed in parallel because they touch different files and share only read-only dependencies from Wave 1.

## Execution Summary

- **T-004** (`zeus_orchestrator.py`): Async dispatcher with `asyncio.Semaphore(max_parallel=3)`, isolated agent workspaces (`agent-workspaces/zeus-agent-{task_id}/`), and `store.lock()`-protected `task.json` batch updates. 5/5 tests passed.
- **T-005** (`zeus_server.py`): FastAPI backend exposing `/status`, `/wave/{n}`, `/agents`, `/events`, `/discussion`, `/graph/mermaid`, `/graph/svg`, and `POST /wave/{n}/approve`. CORS enabled and static `/web` mount预留. 5/5 tests passed.
- **Commit**: `feat(v2-wave2): implement zeus_orchestrator.py and zeus_server.py with tests` → `2c73232`
- **Note**: Agent workspaces generated during `test_dispatch_task_creates_workspace_and_prompt` were inadvertently committed. `.gitignore` should be updated to exclude `.zeus/v2/agent-workspaces/` in a follow-up chore.

## Target Impact

- `multi_agent_efficiency` ↑↑ (orchestrator enables concurrent task dispatch; backend provides real-time observability)
- `developer_adoption_rate` ↑ (API surface ready for GUI and Web consumers)
- `ui_usability` → (backend complete but no frontend yet; Wave 3 will close this gap)
