# AI Log — T-V3-021

## Task
Dashboard 功能补全与后端能力对齐

## Execution Summary
补齐了 Dashboard 中缺失的后端功能 UI，确保前端能够完整覆盖已有的后端 API 能力，同时严格保持现有玻璃拟态深色设计风格。

## Changes

### New Components
- `web/src/components/MetricsPanel.vue`
  - Task Metrics 表格（avg/min/max duration, calls）
  - Bottleneck Top-N 卡片视图
  - Blocked Chains 表格（阻塞源、状态、深度）
- `web/src/components/TaskDetailDrawer.vue`
  - 右侧滑出抽屉展示任务完整字段
  - 支持 depends_on、files、commit_sha、worker_id、heartbeat、scheduled_wave 等

### Enhanced Components
- `Dashboard.vue`
  - 新增 Metrics Tab 与 Health 徽章
  - 集成 TaskDetailDrawer
  - 增加 `/health` 轮询（15s）
- `TasksPanel.vue`
  - 所有状态行增加「详情」按钮入口
- `PhasesPanel.vue`
  - 新增 Phase/Milestone 的创建、编辑、删除功能
  - 玻璃拟态模态框表单
  - Milestone 编辑支持任务多选
- `MailboxPanel.vue`
  - 新增发送消息表单（from/to/message）
  - 发送成功后自动刷新列表
- `EventsPanel.vue`
  - 新增 Live / History 切换
  - History 支持按 event_type / task_id / agent_id 过滤
  - 支持分页加载（Load more）

### i18n
- `en.json` / `zh.json` 新增 metrics、health、detail、history 等文案键

### Build Artifacts
- 重新构建 `scripts/api/static/` 静态资源（vite build 通过，无 TS 错误）

## Tests
- `pytest tests/` — 75/75 passed
- `npm run build` — successful (no TS errors)

## Commit
`e953375`
