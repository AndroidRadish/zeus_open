# AI Log — T-V3-033

## Task
完全移除 task.json 运行时依赖，实现数据库单一事实来源

## Actions
- **DB Schema**: 新增 `PlanHistory` 表（`db/models.py`），支持审计 task/phase/milestone 的变更历史
- **Store 层**: 
  - `store/base.py` 新增 `log_plan_history`, `query_plan_history`, `export_plan` 抽象方法
  - `store/sqlalchemy_base.py` 实现上述方法，支持带/不带运行时字段的计划导出
- **Importer 重构**: `importer.py` 彻底改为"静态字段覆盖、运行时字段保护"的合并策略
  - 新任务默认 `status=pending, passes=False`
  - 已存在任务保留数据库中的 `status`, `passes`, `commit_sha`, `worker_id`
  - 未知字段自动落入 `extra`
- **Exporter 新增**: `exporter.py` 提供 `export_plan_to_json` / `export_plan_to_file`，一键导出当前计划
- **API 扩展**: `api/server.py`
  - 新增 `POST /tasks`, `PUT /tasks/{id}`, `DELETE /tasks/{id}` 完整 CRUD
  - 新增 `GET /plan/export`, `GET /plan/history`
- **CLI 更新**: `run.py`
  - 新增 `--export-plan` 命令，导出当前 DB 计划到 `task.json`
  - 移除 `task.json` 必须存在的限制；无文件时直接以数据库状态运行
  - `_print_status` 不再读取 `task.json`，全部从数据库 meta/task 表获取
- **Prompt 修正**: `.zeus/scripts/zeus_runner.py` 中删除"修改 task.json"指令，改为"写入 zeus-result.json，由 Worker 同步到数据库"
- **Schema 更新**: `.zeus/schemas/task.schema.json` 中 `passes`/`status`/`commit_sha`/`completed_at` 标记为 DEPRECATED in v3
- **测试补充**:
  - `test_import_preserves_runtime_state`: 验证 importer 不会覆盖 DB 已有状态
  - `test_tasks_create_update_delete`: 验证 Task CRUD API
  - `test_plan_export`: 验证默认/带运行时两种导出模式

## Validation
```
93 passed in 144.96s
```

## Commit
`06eb7b4`

## Notes
- 任务整理工作已记录为后续计划，当前架构已支持纯数据库化任务管理
- 若后续需要完全删除 task.json 文件，可继续扩展 Dashboard/CLI 的 CRUD UI
