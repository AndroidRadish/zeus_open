# AI Log — T-V3-023

## Task
Docker sandbox dispatcher 完善与测试

## Actions
- 完善 `dispatcher/docker.py`：
  - 新增 `pids_limit`（`--pids-limit`）cgroup 进程数限制
  - 新增 `blkio_weight`（`--blkio-weight`）IO 权重
  - 新增 `read_only`（默认 `--read-only`）让容器根文件系统只读
  - 新增 `network_disabled`（默认 `--network none`）网络隔离
  - 新增 `security_opts` / `cap_drop`（默认 `--cap-drop ALL`）安全加固
  - 新增 `output_volume_path` 可选输出 volume 隔离：结果写入独立 volume 后复制回 workspace
- 补充 `tests/test_v3_dispatcher_docker.py`：
  - `test_docker_command_builds_with_volumes`：验证所有安全与资源限制标志
  - `test_docker_command_with_output_volume`：验证输出 volume 挂载参数
  - `test_docker_dispatcher_end_to_end`：真实 Docker 运行并验证 `zeus-result.json`
  - `test_docker_dispatcher_with_output_volume`：端到端验证输出 volume 隔离与回拷
  - `test_docker_dispatcher_failure`：验证非零退出码处理
  - `test_docker_dispatcher_timeout`：验证超时杀容器行为

## Validation
```
7 passed in 3.41s
```

## Commit
待提交
