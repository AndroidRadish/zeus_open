# AI Log — T-V3-024

## Task
多镜像拆分与 docker-compose 生产验证

## Actions
- 修复 `Dockerfile` 的 build context 与 COPY 路径：
  - 将 build context 改为项目根目录（`docker-compose.yml` 中 `context: ../..`）
  - 修正 COPY 路径为 `.zeus/v3/scripts`、`.zeus/v3/config.json`
  - 新增 `docker-entrypoint.sh` 并复制到镜像
- 新增 `.zeus/v3/docker-entrypoint.sh`：
  - 自动确保数据库 schema
  - `ZEUS_MODE=scheduler` 时自动写入 `scheduler_target_state=running`
  - `ZEUS_MODE=worker` 时自动写入 `worker_target_count`
- 更新 `docker-compose.yml`：
  - `zeus-api` 增加 `--no-embedded-scheduler`，禁用内嵌调度器（控制平面仅通过 meta 管理）
  - `zeus-scheduler` / `zeus-worker` 注入 `ZEUS_MODE` 环境变量，实现 watch-mode 自动启动
  - 三服务共享 `zeus_state` volume 与 `redis` 队列
- 新增 `tests/test_v3_docker_compose.py`：
  - `test_compose_config_valid`：验证 `docker compose config` 可解析且包含全部服务
  - `test_compose_build`：验证镜像构建成功
  - `test_compose_stack_health`：启动完整 stack，验证 `/health` 与控制平面 `/control/status` 可访问

## Validation
```
tests/test_v3_docker_compose.py::test_compose_config_valid PASSED
tests/test_v3_docker_compose.py::test_compose_build PASSED
tests/test_v3_docker_compose.py::test_compose_stack_health PASSED
```

## Commit
待提交
