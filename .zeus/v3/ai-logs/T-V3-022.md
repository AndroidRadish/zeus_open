# AI Log — T-V3-022

## Task
PostgresStateStore 集成测试与生产验证

## Actions
- 新增 `tests/test_v3_postgres_store.py`，包含 module-scoped Docker PostgreSQL fixture
- Fixture 自动检测 Docker 可用性，启动 `postgres:16-alpine` 容器，等待 `pg_isready`
- 每个测试函数前执行 `drop_all + create_all`，保证数据隔离
- 测试覆盖：
  - `test_postgres_store_task_lifecycle`: upsert/get/list/update/delete
  - `test_postgres_store_quarantine`: quarantine/unquarantine/list/is_quarantined
  - `test_postgres_store_meta_and_events`: meta CRUD + event log/query
  - `test_postgres_store_phase_milestone`: phase/milestone CRUD + 嵌套 enrichment
  - `test_postgres_store_mailbox`: send/list/mark_read
  - `test_postgres_store_metrics`: 通过 MetricsCollector 验证 summary / bottleneck / blocked_chains
  - `test_postgres_store_isolation`: 并发 worker 写入互不干扰
- 修复 PostgreSQL `TIMESTAMP WITH TIME ZONE` 对字符串 `ts` 的严格类型要求（改为传 `datetime` 对象）

## Validation
```
7 passed in 14.09s
```

## Commit
待提交
